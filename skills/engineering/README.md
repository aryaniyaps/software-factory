# Matt Pocock Engineering Skills

Vendored from `https://github.com/mattpocock/skills/tree/main/skills/engineering`.

The factory pins the source revision in `REVISION`. Updates must be reviewed and committed; runtime execution never downloads a mutable branch.

The router uses these flows: `wayfinder`, `to-tickets`, `domain-modeling`, `implement`, `tdd`, `diagnosing-bugs`, and `code-review`.
