# Optional Bearer token for write routes (matches FACTORY_API_TOKEN on the API server)
# VITE_FACTORY_API_TOKEN=

# Temporal Web UI deep links (browse all runs, history, workers)
VITE_TEMPORAL_UI_URL=http://localhost:8080
VITE_TEMPORAL_NAMESPACE=default
