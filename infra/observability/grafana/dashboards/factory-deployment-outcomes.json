{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum(rate(factory_deployment_observations_total[5m])) by (factory_healthy, factory_profile)",
          "legendFormat": "{{factory_profile}} healthy={{factory_healthy}}"
        }
      ],
      "title": "Deployment observations",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "sum(rate(factory_deployment_rollbacks_total[5m])) by (factory_profile)",
          "legendFormat": "{{factory_profile}}"
        }
      ],
      "title": "Deployment rollbacks",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["factory", "deployments"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "Factory Deployment Outcomes",
  "uid": "factory-deployment-outcomes",
  "version": 1
}
