{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum(rate(factory_tool_calls_total[5m])) by (factory_tool_name)",
          "legendFormat": "{{factory_tool_name}}"
        }
      ],
      "title": "Tool call rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(factory_worker_task_latency_ms_bucket[5m])) by (le, factory_node_id))",
          "legendFormat": "{{factory_node_id}} p95"
        }
      ],
      "title": "Worker task latency (p95)",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["factory", "cost"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "Factory Cost",
  "uid": "factory-cost",
  "version": 1
}
