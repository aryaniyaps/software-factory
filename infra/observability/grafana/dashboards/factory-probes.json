{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum(rate(factory_probe_runs_total[5m])) by (factory_probe_id, factory_status)",
          "legendFormat": "{{factory_probe_id}} {{factory_status}}"
        }
      ],
      "title": "Probe run outcomes",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "sum(rate(factory_probe_regressions_total[5m])) by (factory_probe_id)",
          "legendFormat": "{{factory_probe_id}}"
        }
      ],
      "title": "Probe regressions detected",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["factory", "probes"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "Factory Probes",
  "uid": "factory-probes",
  "version": 1
}
